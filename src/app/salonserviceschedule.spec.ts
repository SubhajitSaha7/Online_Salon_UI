import { Salonserviceschedule } from './salonservicescheduledto';

describe('Salonserviceschedule', () => {
  it('should create an instance', () => {
    expect(new Salonserviceschedule()).toBeTruthy();
  });
});
